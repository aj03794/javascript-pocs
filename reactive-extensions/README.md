- An `Observable` is the core type in ReactiveX.
- It serially `pushes items`, known as emissions, through a series of operators until it finally arrives at an `Observer`, where they are `consumed`.

